{
  "name": "phyllis-website",
  "version": "1.0.0",
  "description": "Phyllis 的个人网站",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist -p 3000"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
